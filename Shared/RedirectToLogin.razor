@inject NavigationManager Navigation

@code {

    protected override void OnInitialized()
    {
        Navigation.NavigateTo(uri: $"authentication/login?returnUrl={Uri.EscapeDataString(stringToEscape: Navigation.Uri)}");
    }

}